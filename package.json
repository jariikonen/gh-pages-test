{
  "name": "gh-pages-test",
  "version": "0.0.0",
  "description": "Testing gh-pages publishing from a monorepo",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "workspaces": [
    "packages/static-site"
  ]
}
